# SVG
Experiments with SVG charting

This repository contains some early experiementation for a SVG charting library.

Use the "server.sh" script to start a simple web server and open the "chart.html" file.
